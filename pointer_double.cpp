#include <iostream>
using namespace std;

int main()
{

    // int i = 5;
    // int *p = &i;
    // int **p2 = &p;

    // cout << "p " << p << endl;
    // cout << "Address of p " << &p << endl;
    // cout << "Address of p " << *p << endl;

    // cout << "* p2 " << *p2 << endl;
    // cout << "** p2 " << *(*p2) << endl;

    // Exercise

    // int first = 8;
    // int *p = &first;
    // cout << (*p)++ << " ";
    // cout << first << endl;

    // int *p = 0;
    // int first = 110;
    // *p = first;//cannot write we need to writ p=&i;
    // cout << (*p) << " " << endl;

    // int first = 8;
    // int second = 11;
    // int *third = &second;
    // first = *third;
    // *third = *third + 2;
    // cout << first << " " << second << endl;

    // float f = 12.5;
    // float p = 21.5;
    // float *ptr = &f;
    // (*ptr)++;
    // *ptr = p;
    // cout << *ptr << " " << f << " " << p << endl;

    // int arr[5];
    // int *ptr ;
    // cout<<sizeof(arr)<<" "<<sizeof(*ptr)<<endl;

    // int arr[6]={11,12,31};
    // cout<<arr<<" "<<&arr<<endl;

    // int arr[6]={11,12,31};
    // cout<<(arr+1)<<endl;

    // int arr[6]={11,12,31};
    // int *p = arr;
    // cout<<p[2]<<endl;

    // int arr[]={11,12,13,14,15};
    // cout<<*arr<<" "<<*(arr+3);

    // int arr[] = {11,21,31,41};
    // int *ptr = arr++; // we cannot update array when poiner comes
    // cout<<*ptr<<endl;

    // char ch = 'a';
    // char * ptr = &ch;
    // ch++;
    // cout<<*ptr<<endl;

    // char arr[] = "abcehs";
    // char *p = &arr[0];
    // cout << p << endl;   
    // p++;
    // cout << p << endl;

    

    return 0;
}